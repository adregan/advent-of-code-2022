 solution←{
     in←⍎⍤(','@('-'=⊢))¨⎕CSV ⍵

     incl←≥⍥⊃∧≤⍥(2⊃⊢)
     ⎕←'part one:',+/∨/2 incl/⍤1⊢(⊢,(1⌷⍉))in

     ovr←{((⊃⍺≥⊃⍵)∧(⊃⍺≤2⊃⍵))∨((2⊃⍺≥⊃⍵)∧(2⊃⍺≤2⊃⍵))}
     ⎕←'part two:',+/∨/2 ovr/⍤1⊢(⊢,(1⌷⍉))in
 }
